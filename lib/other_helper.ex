defmodule OtherHelper do

   # def nestedLoop(matrix = [ row | tail ])
   # when is_list(row)
   # do
   #   IO.puts "external"
   #   array_put row
   #   [ transpose(row) | transpose(tail) ]
   # end
   #
   # def nestedLoop(row = [ head | tail ]) do
   #   IO.puts "internal"
   #   IO.puts head
   #   [ head | transpose(tail) ]
   # end
   # def nestedLoop([]) do
   #   []
   # end
end
